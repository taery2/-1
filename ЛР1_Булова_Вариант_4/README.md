# Лабораторная работа №1 — вариант 4

**Студент:** Осипова Анна  
**Вариант:** 4  
**Язык:** Python 3.10+

## Исходные данные

- `a = 0.25`
- `b = 15`
- `c = 10`
- `eps = 1e-4`
- критерий остановки для корней: **по невязке**
- `x0 = 1`
- дополнительное требование: **Д3 — нормализация аргумента для `ln(c)`**

## Запуск

```bash
python main.py
```

Дополнительное сравнение Д3:

```bash
python experiment.py
```

Вычислительные функции не используют `math.sqrt`, `math.log` или аналогичные функции для получения основного результата. Стандартная библиотека используется только для эталонной проверки и определения степени двойки при нормализации.
